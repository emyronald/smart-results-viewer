// types.ts
export interface Student {
  id: string;
  name: string;
  class: string;
}

export interface Teacher {
  id: string;
  name: string;
  subject: string;
}

export interface Session {
  id: string;
  name: string;
}
